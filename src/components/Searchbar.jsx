import { Input } from "semantic-ui-react";

const Searchbar = () => {
  return (
    <Input
      icon="search"
      placeholder="Sök efter titlar..."
      className="searchbar"
    />
  );
};

export default Searchbar;
